# 功能規格筆記

這份文件不是程式碼，而是重構前的功能說明書。

目的：
- 先確認目前系統每個核心流程「應該怎麼運作」
- 讓後續重構時有依據，不會一邊改一邊猜
- 方便逐條確認哪些地方需要修正

---

## 1. Login

### 觸發條件
- 使用者在登入頁輸入帳號與密碼後按下登入

### 成功條件
- 帳號存在
- 密碼正確

### 失敗條件
- 帳號未輸入
- 密碼未輸入
- 帳號不存在
- 密碼錯誤
- 資料庫查詢失敗

### 資料異動
- 寫入 Session
- 目前預計最少需要：
  - `UserId`
  - `UserName`
  - `UserRole`

### 回傳結果
- 若為管理者，導向設備管理頁
- 若為一般使用者，導向設備預約頁
- 若失敗，停留在登入頁並顯示錯誤訊息

### 備註
- 後續若要擴充完整專案，應先定義「CurrentUser 最小必要資訊」
- 不建議把過多使用者資料直接塞進 Session

---

## 2. CreateReservation

### 觸發條件
- 已登入的使用者在設備頁按下預約按鈕

### 成功條件
- 使用者已登入
- 設備存在
- 設備在開放時間內
- 使用者沒有任何其他進行中的預約
- 使用者沒有同設備的排隊記錄
- 設備目前尚有可用名額

### 失敗條件
- 未登入
- 設備不存在
- 不在開放時間內
- 已有其他進行中的預約
- 已在同設備排隊中
- 資料庫處理失敗

### 資料異動
- 新增一筆 `Reservations`
- 預約狀態為使用中
- 記錄：
  - `EquipmentId`
  - `UserId`
  - `StartTime`
  - `ReservationTime`
  - `Status`

### 回傳結果
- 成功時回傳「預約成功，立即開始使用」
- 失敗時回傳對應錯誤訊息

### 備註
- 這個流程只處理「設備有空位，可直接開始使用」
- 如果設備已滿，應轉到 `JoinWaitingQueue`

---

## 3. CancelReservation

### 觸發條件
- 已登入的使用者對自己「尚未開始、但已成立」的預約按下取消

### 成功條件
- 使用者已登入
- 該預約存在
- 該預約屬於目前使用者
- 該預約屬於「預約已成立但尚未開始」的狀態

### 失敗條件
- 未登入
- 預約不存在
- 預約不屬於目前使用者
- 該筆資料其實是排隊資料，應改走 `CancelQueue`
- 該預約已開始使用，應改走 `EndUsage`
- 預約狀態不可取消
- 資料庫處理失敗

### 資料異動
- 更新 `Reservations` 的狀態為取消
- 若取消後設備產生空位，需確認是否觸發排隊遞補

### 回傳結果
- 成功時回傳「取消成功」
- 失敗時回傳「取消失敗」或具體原因

### 備註
- 這個流程只處理「已成立的預約取消」
- 若是取消排隊，應統一由 `CancelQueue` 處理，避免流程重複
- 需要明確定義系統中是否真的存在「尚未開始但已成立」的預約狀態

---

## 4. EndUsage

### 觸發條件
- 已登入的使用者在使用完成後按下結束使用

### 成功條件
- 使用者已登入
- 該預約存在
- 該預約屬於目前使用者
- 該預約目前狀態為使用中

### 失敗條件
- 未登入
- 預約不存在
- 預約不屬於目前使用者
- 預約不是使用中狀態
- 資料庫處理失敗

### 資料異動
- 更新 `Reservations`
- 寫入：
  - `EndTime`
  - `Status = Completed`
- 若設備釋出空位，需處理排隊遞補

### 回傳結果
- 成功時回傳「結束使用成功」
- 失敗時回傳「結束使用失敗」或具體原因

### 備註
- 這個流程和過期自動完成流程的責任要明確切開
- 手動結束與系統自動結束，應共享相同核心規則

---

## 5. JoinWaitingQueue

### 觸發條件
- 已登入的使用者預約設備時，設備已滿

### 成功條件
- 使用者已登入
- 設備存在
- 設備在開放時間內
- 使用者沒有同設備進行中預約
- 使用者沒有同設備排隊記錄
- 設備目前已滿，無法立即使用

### 失敗條件
- 未登入
- 設備不存在
- 不在開放時間內
- 已有同設備進行中預約
- 已在同設備排隊中
- 資料庫處理失敗

### 資料異動
- 新增一筆 `WaitingQueue`
- 記錄：
  - `EquipmentId`
  - `UserId`
  - `QueueTime`
  - `Position`

### 回傳結果
- 成功時回傳：
  - 已加入排隊
  - 排隊順位
  - 預估等待時間
  - 預估開始時間
- 失敗時回傳對應錯誤訊息

### 備註
- 排隊與預約雖然有關聯，但應視為獨立流程
- 未來 API 化時，建議和 `CreateReservation` 拆成清楚的服務邏輯

---

## 6. CancelQueue

### 觸發條件
- 已登入的使用者對自己的排隊記錄按下取消排隊

### 成功條件
- 使用者已登入
- 排隊記錄存在
- 排隊記錄屬於目前使用者

### 失敗條件
- 未登入
- 排隊記錄不存在
- 排隊記錄不屬於目前使用者
- 資料庫處理失敗

### 資料異動
- 刪除一筆 `WaitingQueue`
- 重新計算該設備排隊順位

### 回傳結果
- 成功時回傳「取消排隊成功」
- 失敗時回傳「取消排隊失敗」或具體原因

### 備註
- 取消排隊後是否立即觸發遞補，需要與排隊機制設計一起確認

---

## 7. AutoCompleteExpiredReservations

### 觸發條件
- 系統背景服務定期掃描進行中的預約
- 或管理員手動觸發清理

### 成功條件
- 找出已超過可用時間的使用中預約
- 正確將這些預約轉為已完成
- 若設備有候補者，能正確推進排隊

### 失敗條件
- 背景服務執行失敗
- 資料庫查詢失敗
- 狀態更新失敗
- 排隊遞補失敗

### 資料異動
- 更新逾時的 `Reservations`
- 寫入：
  - `EndTime`
  - `Status = Completed`
- 釋出設備名額後，可能新增新的使用中預約或調整排隊

### 回傳結果
- 背景服務模式：寫入 log
- 手動觸發模式：回傳清理成功或失敗訊息

### 備註
- 這個流程屬於系統規則，不應分散在多個 Controller 重複處理
- 後續重構時，建議收斂成單一服務負責

---

## 後續確認重點

接下來建議逐條確認以下問題：
- `UserId` 應該用資料表主鍵，還是目前的 `UserName`
- 預約、排隊、取消、結束使用的狀態轉換是否完整
- 哪些資料應放 Session，哪些只保留在資料庫查詢
- 排隊遞補的觸發時機是否唯一且一致
- MVC 頁面流程與未來 API 流程要如何共用同一套規則

---

## 已確認的細化需求

### 1. 支援預約未來時段

- 系統需支援預約未來時段，不只限於立即使用
- 預約時間粒度為 15 分鐘
- 使用者可選擇明確時段，例如 `10:15`
- 因此後續系統需區分以下概念：
  - 立即使用
  - 未來預約
  - 即時排隊
  - 預約排隊

### 2. 密碼找回機制

- 登入功能需支援「忘記密碼 / 密碼找回」
- 使用者需輸入註冊時設定的 Email
- 系統透過我方設定的 Email 帳號寄送隨機 6 碼數字驗證碼
- 驗證碼有效時間為 10 分鐘
- 使用者在期限內輸入正確驗證碼後，可直接設定新密碼
- 此功能希望以 Python 處理寄信與驗證碼流程

### 3. 排隊規則

- 排隊屬於即時排隊
- 優先順序規則：
  - 使用中
  - 即時排隊
  - 預約排隊
- 若使用者預約未來時段，但系統推算到該時段時設備仍無法供其使用，則此使用者不可插隊
- 該使用者應併入當下最後方排隊隊列

### 4. 排隊規則範例

- 設備 A
- 單次使用時間：30 分鐘
- 同時可使用人數：3 人
- 某使用者預約 1 小時後的時段
- 目前已有 7 位排隊者
- 系統推算到 1 小時後，仍有 1 人尚未排到
- 結果：
  - 該預約者不能直接開始使用
  - 該預約者需排入當下最後方隊列

### 5. 管理者權限

- 管理者可對設備進行新增、修改、刪除與設定
- 管理者可強制將會員結束使用
- 後續需再細化：
  - 管理者是否可代替會員取消預約
  - 管理者是否可調整排隊順序
  - 管理者是否可直接清空排隊

---

## 未來擴充方向

目前的預約功能只是整個專案的一部分。

專案整體定位：
- 專案名稱為「智能社區」
- 目標是逐步擴充成完整的社區智慧管理系統

因此後續重構時，不能只把目前設備預約功能做得更乾淨，還要預留可擴充成多模組系統的空間。

### 預計新增模組

#### 1. 快遞寄送

- 管理者協助接收住戶快遞
- 管理者協助寄送住戶快遞
- 後續可能涉及：
  - 快遞狀態追蹤
  - 領取通知
  - 寄送紀錄

#### 2. 房間管理系統

- 對住戶與成員進行登記
- 管理水費、電費、管理費
- 支援費用調整、紀錄與通知
- 後續可能涉及：
  - 房號與住戶綁定
  - 帳單生成
  - 繳費紀錄
  - 欠費提醒

#### 3. 公告通知

- 作為居委會、里長或行政通知的發布位置
- 後續可能涉及：
  - 公告分類
  - 發布時間
  - 置頂與過期下架
  - 指定住戶或全體通知

---

## 對重構方向的影響

根據目前已確認需求，後續重構不能只做單一預約系統整理，還需要考慮以下方向：

- 架構需從單一功能專案，調整為可擴充的多模組專案
- 預約系統需支援更完整的狀態流轉
- 帳號系統需支援 Email 驗證與密碼找回
- 權限系統至少要區分一般住戶與管理者
- 資料表設計需能承接後續模組，而非只服務目前設備預約
- 後續 API 化時，應以模組切分，而不是以頁面切分

---

## 使用方式

你可以直接在這份文件上做 3 種確認：
- 保留：代表這條規則沒問題
- 修改：代表這條規則要改
- 補充：代表這條規則缺少實際需求

等你確認完，我們下一步再把它轉成：
- 資料表重構清單
- Service 拆分清單
- API 化路線圖
