# 預約狀態流轉筆記

這份文件用來整理「設備預約模組」未來應有的狀態設計。

目的：
- 區分立即使用、未來預約、即時排隊、預約排隊
- 釐清每個狀態可以做什麼事
- 幫助後續資料表設計、API 設計、Service 拆分

---

## 1. 為什麼需要狀態流轉

目前系統的預約邏輯比較偏向：
- 能用就直接開始使用
- 不能用就進入排隊

但依照目前已確認需求，未來系統需要支援：
- 立即使用
- 預約未來時段
- 即時排隊
- 預約排隊
- 管理者強制結束使用

所以之後不能只用「有預約 / 沒預約」來思考，必須明確定義每個狀態。

---

## 2. 建議的核心狀態

以下是目前建議的第一版狀態：

### 1. Scheduled

意義：
- 使用者已成功預約未來時段
- 但尚未開始使用

範例：
- 早上 7:00 預約 11:00 的設備

此狀態可執行：
- 使用者取消預約
- 管理者取消預約
- 等待到預約時段時進入下一步判斷

---

### 2. InProgress

意義：
- 使用者目前正在使用設備

範例：
- 使用者立即預約成功並開始使用
- 使用者從排隊遞補成功後開始使用
- 使用者在預約時段到達時成功開始使用

此狀態可執行：
- 使用者主動結束使用
- 管理者強制結束使用
- 系統自動判定逾時結束

---

### 3. QueueingNow

意義：
- 使用者現在就想使用設備
- 但設備已滿，所以進入即時排隊

範例：
- 使用者現在按下預約，但目前設備無空位

此狀態可執行：
- 使用者取消排隊
- 系統排到時轉成 `InProgress`

---

### 4. QueueingFromReservation

意義：
- 使用者原本有未來預約
- 但到達預約時段時，設備仍無法提供使用
- 因此轉入排隊，且排在當下最後方

範例：
- 使用者預約 11:00
- 到 11:00 時，前面仍有尚未排完的即時排隊者
- 使用者併入最後方排隊

此狀態可執行：
- 使用者取消排隊
- 系統排到時轉成 `InProgress`

---

### 5. Completed

意義：
- 該次使用已正常結束

範例：
- 使用者自己按下結束使用
- 管理者強制結束使用
- 系統自動逾時完成

此狀態可執行：
- 僅供查詢歷史紀錄

---

### 6. Cancelled

意義：
- 預約或排隊已被取消

範例：
- 使用者取消未來預約
- 使用者取消即時排隊
- 使用者取消預約排隊
- 管理者取消某筆預約

此狀態可執行：
- 僅供查詢歷史紀錄

---

## 3. 建議的狀態流轉

### 情境 A：立即使用成功

`建立請求 -> InProgress -> Completed`

說明：
- 使用者現在要用設備
- 系統檢查設備有空位
- 直接開始使用
- 最後由使用者、管理者或系統結束

---

### 情境 B：立即使用但設備已滿

`建立請求 -> QueueingNow -> InProgress -> Completed`

說明：
- 使用者現在要用設備
- 但設備已滿
- 先進入即時排隊
- 排到後開始使用
- 最後完成

也可能：

`建立請求 -> QueueingNow -> Cancelled`

說明：
- 使用者還沒排到前先取消排隊

---

### 情境 C：未來預約成功，準時開始使用

`建立未來預約 -> Scheduled -> InProgress -> Completed`

說明：
- 使用者先預約未來時段
- 到達預約時間時剛好有空位
- 直接開始使用

也可能：

`建立未來預約 -> Scheduled -> Cancelled`

說明：
- 使用者在開始前取消預約

---

### 情境 D：未來預約成功，但到點後仍無法使用

`建立未來預約 -> Scheduled -> QueueingFromReservation -> InProgress -> Completed`

說明：
- 使用者有預約未來時段
- 但到時設備仍被占用，或即時排隊尚未消化完
- 使用者不得插隊
- 需併入最後方排隊
- 排到後開始使用

也可能：

`建立未來預約 -> Scheduled -> QueueingFromReservation -> Cancelled`

說明：
- 使用者轉入排隊後又取消

---

## 4. 狀態之間的轉換規則

### Scheduled -> InProgress

條件：
- 到達預約開始時間
- 設備有可用名額

---

### Scheduled -> QueueingFromReservation

條件：
- 到達預約開始時間
- 設備沒有可用名額
- 即時排隊尚未消化完

---

### QueueingNow -> InProgress

條件：
- 前面排隊者已完成
- 設備釋出空位
- 輪到該使用者

---

### QueueingFromReservation -> InProgress

條件：
- 設備釋出空位
- 輪到該使用者

---

### InProgress -> Completed

條件：
- 使用者主動結束
- 管理者強制結束
- 系統逾時自動結束

---

### Scheduled / QueueingNow / QueueingFromReservation -> Cancelled

條件：
- 使用者主動取消
- 或管理者取消

---

## 5. 建議先確認的業務規則

這些規則會直接影響後續資料表與程式設計，建議先確認：

### 1. 一位使用者同時可不可以有多筆未來預約

目前已確認：
- 不可有其他進行中的預約

但還要再確認：
- 可不可以同時有多筆 `Scheduled`
- 可不可以一邊 `Scheduled`，一邊 `QueueingNow`

---

### 2. 預約時段最早與最晚可預約多久

例如：
- 最早可預約現在之後 15 分鐘
- 最晚可預約 7 天內

這會影響畫面設計與資料驗證。

---

### 3. 到了預約時間是否需要報到

可能做法：
- 不需要報到，時間到系統自動判定
- 需要報到，逾時未報到則取消或轉排隊

這會影響 `Scheduled -> InProgress` 的轉換方式。

---

### 4. 預約排隊是否和即時排隊共用同一條隊列

目前傾向：
- 共用同一條最終隊列
- 但來源不同

也就是：
- `QueueingNow` 是立即使用排隊
- `QueueingFromReservation` 是預約到時未能使用後轉入的排隊

---

### 5. 管理者能做到什麼程度

目前已知：
- 管理者可設定與修改設備
- 管理者可強制將會員結束使用

還可再確認：
- 是否能取消未來預約
- 是否能取消排隊
- 是否能手動調整順位

---

## 6. 對資料設計的直接影響

若要支援上述狀態，後續資料表大概率需要以下資訊：

- 預約建立時間
- 預約開始時間
- 預約結束時間
- 實際開始時間
- 實際結束時間
- 狀態
- 排隊來源
- 排隊順位
- 取消時間
- 取消原因
- 是否為管理者強制結束

這表示目前的資料表與模型大機率不足，需要後續重新盤點。

---

## 7. 目前建議

重構時，先不要急著直接改 Controller。

比較安全的順序是：
1. 先把狀態定義清楚
2. 再整理資料表欄位
3. 再拆 Service
4. 最後才把 MVC 與 API 接口接上去

---

## 8. 下一步可做的事

你確認這份狀態流轉筆記後，我們可以接著做其中一個：

1. 整理「預約資料表應該有哪些欄位」
2. 整理「排隊資料表應該有哪些欄位」
3. 畫出「預約模組的資料關係」
4. 反推目前程式哪些地方不符合這套狀態設計
